import { StoreListMetadata } from "@/lib/metadata";
import StoreList from "./storelist"; 

export const metadata = StoreListMetadata;

export default function StoreListPage() {
  return <StoreList />;
}
